parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phplint: ~
        git_blacklist:
            keywords:
            - "die("
            - "var_dump("
            - "exit;"
        phpcsfixer: ~
        phpcs:
            standard: "PSR2"
        phpmd: ~