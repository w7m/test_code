{% extends 'insurance/sinister/skeleton_page/baseSinistre.html.twig' %}

{% block body %}
    <div class="card">
        <div class="card-header bg-info">
            <h4 style="color: white"><i class="fas fa-user-plus"></i> Ajouter un nouveau expert</h4>
        </div>
        <div class="d-flex card-body">
            <div class="col-6">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="Ajouter Expert">
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
