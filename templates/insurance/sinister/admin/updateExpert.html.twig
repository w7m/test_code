{% extends 'insurance/sinister/skeleton_page/baseSinistre.html.twig' %}

{% block body %}
    <div class="card">
        <h4 class="card-header bg-info" style="color: white"><i class="fas fa-user-edit"></i>Modifier Expert</h4>
        <div class="d-flex card-body">
            <div class="col-6">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="Modifier Expert">
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
